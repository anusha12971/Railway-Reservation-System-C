# Railway-Reservation-System-C
A console-based Railway Reservation System in C that supports ticket booking, automatic PNR generation, seat allocation, cancellation, and PNR search. Uses binary file handling for persistent storage and displays real-time seat availability through a seat-map interface.
